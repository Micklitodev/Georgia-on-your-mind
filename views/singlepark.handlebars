<div>
    <h2>{{name}} </h2>
    <div>{{county}} </div>
    <img src="{{image}}">
    <p> {{content}} </p>

  {{#each review as |review|}}
    <div>
      <p>{{review.rating}}</p>
      <p>{{review.content}}</p>
    </div>
  {{/each}}

</div>

 <script src="../js/review.js"></script> 